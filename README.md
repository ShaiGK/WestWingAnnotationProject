# West Wing Annotation Project

## Setup

1. Install dependencies
2. Run `playwright install`
3. Run `python scrape_scripts.py` after choosing the desired number of seasons
4. Run `python dialogue_parser.py` after choosing min and max back-and-forths